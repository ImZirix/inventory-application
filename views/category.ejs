<div class="hero">
  <% items.forEach(item => { %>
    <div class="card">
      <h4><%= item.name %></h4>
      <p><%= item.description %></p>
      <p>Stock: <%= item.stock %></p>
      <p>Price: <%= item.price %>$</p>
    </div>
    <% }) %>
</div>
